export interface IBarcodeProp {
    value: string,
    options: {
        background?: string,
        width?: number,
        height?: number,
        lineColor?: string,
        displayValue?: boolean,
    }
}
