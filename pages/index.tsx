import React from 'react';
import type { NextPage } from 'next';
import Layout from '../src/components/Layout';

const Home: NextPage = () => (
  <Layout>
    Juan
  </Layout>
);

export default Home;
